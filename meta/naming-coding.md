# Naming & Coding

Private fields:
- `_camelCase`. Injected fields use `[Inject]` on private fields.

Public API:
- Methods/properties: PascalCase.

ScriptableObjects:
- Data-only (configs, definitions, static data).
- No runtime state, events, or business logic.
