# Default Workflow Pipeline

1) Lead Architect → blueprint (no code).
2) Implementation → code per blueprint.
3) Integration → DI wiring, buses, contexts.
4) QA → light review, lifecycle/DI enforcement.
5) Git → commit plan + memory updates.
